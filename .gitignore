# dependencies
node_modules

# build output
dist
dist-ssr
*.local

# env
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log

# local-only Claude Code settings (keep shared launch.json tracked)
.claude/settings.local.json

# PWA generated service worker (regenerated on build)
dev-dist
